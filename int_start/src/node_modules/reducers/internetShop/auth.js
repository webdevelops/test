const initialState = {
  token: null,
  error: ''
}

export default (state = initialState, { type, payload }) => {
  switch (type) {
    case 'AUTH_SUCCESS':
      return {...state, token: payload}

    case 'AUTH_FEILURE':
      return {...state, error: payload}

    default:
      return state
  }
}